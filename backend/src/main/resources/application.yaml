ktor:
  deployment:
    port: "$PORT:8080"
  application:
    modules:
      - com.kanjimasta.ApplicationKt.module

database:
  url: "$DATABASE_URL:"

supabase:
  url: "$SUPABASE_URL:"

aiWorker:
  baseUrl: "$AI_WORKER_URL:"

claude:
  apiKey: "$CLAUDE_API_KEY:"

gemini:
  apiKey: "$GEMINI_API_KEY:"
