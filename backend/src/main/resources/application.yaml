ktor:
  deployment:
    port: "$PORT:8080"
  application:
    modules:
      - com.kanjimasta.ApplicationKt.module

firebase:
  projectId: "$FIREBASE_PROJECT_ID:kanji-masta"
  authEmulatorHost: "$FIREBASE_AUTH_EMULATOR_HOST:"
  dataConnectHost: "$FIREBASE_DATACONNECT_HOST:127.0.0.1:9399"
  functionsHost: "$FIREBASE_FUNCTIONS_HOST:127.0.0.1:5001"

claude:
  apiKey: "$CLAUDE_API_KEY:"

gemini:
  apiKey: "$GEMINI_API_KEY:"
